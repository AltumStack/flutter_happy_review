name: happy_review
description: "A strategic in-app review library for Flutter that triggers review prompts at happy moments, not arbitrary launch counts."
version: 0.1.0
publisher: altumstack.com
homepage: https://github.com/AltumStack/flutter_happy_review
repository: https://github.com/AltumStack/flutter_happy_review
issue_tracker: https://github.com/AltumStack/flutter_happy_review/issues

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  in_app_review: ^2.0.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
